{"id": "cfdec9b7-a84c-44f1-956f-3261a5680f92", "code": "#include <cstdint>\n#include <unordered_map>\n#include <set>\n\ntypedef uint64_t obj_id_t;\n\n// LFU-Size: Evict by freq, break ties with size (smaller evicted first), then by recency\nclass CacheManager {\nprivate:\n    struct LFUNode {\n        uint64_t freq;\n        uint64_t size;\n        uint64_t timestamp;\n        obj_id_t obj_id;\n        LFUNode(uint64_t f, uint64_t s, uint64_t ts, obj_id_t id) : freq(f), size(s), timestamp(ts), obj_id(id) {}\n        // Evict least freq, then smallest size, then oldest\n        bool operator<(const LFUNode& o) const {\n            if (freq != o.freq) return freq < o.freq;\n            if (size != o.size) return size < o.size;\n            if (timestamp != o.timestamp) return timestamp < o.timestamp;\n            return obj_id < o.obj_id;\n        }\n    };\n    std::unordered_map<obj_id_t, std::pair<LFUNode, std::set<LFUNode>::iterator>> map;\n    std::set<LFUNode> lfu_set;\n    uint64_t time_counter = 0;\n\npublic:\n    bool find(obj_id_t obj_id) {\n        auto it = map.find(obj_id);\n        if (it == map.end()) return false;\n        LFUNode node = it->second.first;\n        lfu_set.erase(it->second.second);\n        node.freq += 1;\n        node.timestamp = ++time_counter;\n        auto iter = lfu_set.insert(node).first;\n        it->second = std::make_pair(node, iter);\n        return true;\n    }\n\n    void insert(obj_id_t obj_id, uint64_t obj_size) {\n        auto it = map.find(obj_id);\n        if (it != map.end()) {\n            find(obj_id);\n            return;\n        }\n        LFUNode node(1, obj_size, ++time_counter, obj_id);\n        auto iter = lfu_set.insert(node).first;\n        map[obj_id] = std::make_pair(node, iter);\n    }\n\n    obj_id_t evict() {\n        if (lfu_set.empty()) return 0;\n        auto it = lfu_set.begin();\n        obj_id_t victim = it->obj_id;\n        map.erase(victim);\n        lfu_set.erase(it);\n        return victim;\n    }\n};", "language": "python", "parent_id": "6585c7e4-9d58-4886-943d-a483fbe3bf39", "generation": 2, "timestamp": 1763752670.0727713, "iteration_found": 13, "metrics": {"combined_score": -Infinity}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 59 lines with 60 lines", "parent_metrics": {"combined_score": -Infinity}, "island": 0}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}